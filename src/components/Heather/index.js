import React from 'react';

// import {HiMagnifyingGlass} from "react-icons/hi"
import { Input } from '../Input';

export const Heather = ({searchCard}) => {

  return (
    <div>
      <div>
        <Input searchCard={searchCard}/>
        <button>oie</button>
      </div>
    </div>
  );
};
